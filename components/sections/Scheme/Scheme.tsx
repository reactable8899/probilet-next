'use client';
import React from 'react';
import { useTranslations } from 'next-intl';

interface IScheme {}

export const Scheme: React.FC<IScheme> = (props) => {
  const t = useTranslations();

  return <></>;
};
